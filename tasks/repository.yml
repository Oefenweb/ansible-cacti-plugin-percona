# tasks file for cacti-plugin-percona
---
- name: repository | install | dependencies
  apt:
    name:
      - dirmngr
    state: "{{ apt_install_state | default('latest') }}"
    update_cache: true
    cache_valid_time: "{{ apt_update_cache_valid_time | default(3600) }}"
  when:
    - ansible_distribution == 'Debian'
    - ansible_distribution_version is version('9', '>=')
  tags:
    - cacti-plugin-percona-repository-install
    - cacti-plugin-percona-repository-install-dependencies

- name: repository | add public key
  apt_key:
    id: 9334A25F8507EFA5
    keyserver: keyserver.ubuntu.com
    state: present
  tags:
    - cacti-plugin-percona-repository-public-key

- name: repository | add
  apt_repository:
    repo: "{{ item.type }} {{ item.url }} {{ item.component }}"
    state: present
    update_cache: true
    mode: 0644
  with_items: "{{ cacti_plugin_percona_repositories }}"
  tags:
    - cacti-plugin-percona-repository-add

- name: repository | apt-pin packages
  copy:
    src: etc/apt/preferences.d/00percona.pref
    dest: /etc/apt/preferences.d/00percona.pref
    owner: root
    group: root
    mode: 0644
  tags:
    - cacti-plugin-percona-repository-apt-pin
