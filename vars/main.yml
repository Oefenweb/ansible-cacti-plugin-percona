# vars file for cacti-plugin-percona
---
cacti_plugin_percona_repositories:
  - type: deb
    url: "http://repo.percona.com/apt {{ ansible_lsb.codename }}"
    component: main
  - type: deb-src
    url: "http://repo.percona.com/apt {{ ansible_lsb.codename }}"
    component: main

cacti_plugin_percona_dependencies:
  - percona-cacti-templates
